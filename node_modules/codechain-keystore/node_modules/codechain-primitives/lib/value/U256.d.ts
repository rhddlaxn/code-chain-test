/// <reference types="node" />
import { BigNumber } from "bignumber.js";
/**
 * Handles 256-bit unsigned integers. Used to express nonce, asset amount, etc.
 */
export declare class U256 {
    static MAX_VALUE: BigNumber;
    static fromBytes(buffer: Buffer): U256;
    static check(param: any): boolean;
    static ensure(param: U256 | string | number | BigNumber): U256;
    private static checkString;
    value: BigNumber;
    constructor(value: number | string | BigNumber);
    increase(): U256;
    toEncodeObject(): string | number;
    rlpBytes(): Buffer;
    isEqualTo(rhs: U256): boolean;
    toString(base?: 10 | 16): string;
}
